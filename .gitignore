# Dependencies
node_modules/

# Build output
dist/

# Android build artifacts (keep android/ itself — it's part of the project)
android/.gradle/
android/app/build/
android/build/
android/local.properties

# Capacitor generated config (regenerated by cap sync)
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json

# IDE
.idea/
*.iml
.vscode/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Claude Code — commit settings.json (project permissions), exclude ephemeral dirs
.claude/plans/
.claude/memory/

# Dev-only, never shipped or committed (kept locally for reference/iteration)
# DEPLOY_NOTES.md — Play Store deploy walkthrough (see CLAUDE.md pointer)
DEPLOY_NOTES.md
# weapontest.html + src/weapontest.ts — PULSAR weapon visual harness
weapontest.html
src/weapontest.ts
# .playwright-mcp/ — Playwright screenshot/console artifacts
.playwright-mcp/
