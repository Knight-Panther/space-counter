import * as Phaser from 'phaser';

export class HUDScene extends Phaser.Scene {
  constructor() {
    super({ key: 'HUDScene' });
  }

  create(): void {
    // Score, lives, and letter-choice buttons drawn here
  }
}
